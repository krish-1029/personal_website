export default function MusicPage() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-12 text-white">
      <h1 className="mb-4 text-3xl font-bold">Music</h1>
      <p className="text-white/80">
        Embed your tracks (e.g., SoundCloud, Spotify) or host audio files in
        `public/`. We can add a simple audio player and album art grid.
      </p>
    </main>
  );
} 